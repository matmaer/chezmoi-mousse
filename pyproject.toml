[project]
name = "chezmoi-mousse"
version = "0.1.0"
description = "TUI for chezmoi operation convenience"
readme = "README.md"
license = { file = "LICENSE" }
authors = [
    { name = "mm", email = "mm@sofa.local" }
]
requires-python = ">=3.12"
dependencies = [
    "textual>=1.0.0",
]

[project.scripts]
chezmoi-mousse = "chezmoi_mousse:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
